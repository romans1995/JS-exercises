//  hey I found 2 ways to get to the solution one after another and just wanted to share :D 
const num = -8
let absolute;


num < 0 ? absolute = -num :absolute = num + (num * -2);

absolute = num < 0 ? -num : num;

